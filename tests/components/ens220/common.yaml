i2c:
  - id: i2c_ens220
    scl: ${scl_pin}
    sda: ${sda_pin}

sensor:
  - platform: ens220
    temperature:
      name: ENS220 Temperature
    pressure:
      name: ENS220 Pressure
    update_interval: 15s
